BEGIN
   GET value for base and height in variable
   CALCULATE the product of base and height
   b = int(input("Input the base : "))
   h = int(input("Input the height : "))
   area = b*h/2
   print("area = ", area)
END
